mutation ApproveExpenseEdit($id: ID!) {
  approveExpenseEdit(id: $id) {
    id
    title
    description
    amount
    receiptUrl
    expenseDate
    status
    category {
      id
      name
    }
    updatedAt
  }
}
